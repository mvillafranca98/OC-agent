# Mortgage Signals Agent — supporting scripts and connectors
# Use for: exporters, signal scoring, enrichment clients, CSV/Sheet tooling
# OpenClaw itself is the runtime (install separately); this file is for Python helpers.

# CSV and data handling
pandas>=2.0.0

# HTTP and APIs (search, enrichment)
requests>=2.28.0

# Email verification / enrichment (use approved provider, e.g. Hunter.io)
# hunter.io does not have an official Python package; use requests with their API
# Optional: google-api-python-client for Google Sheets export
# google-api-python-client>=2.0.0

# Env and config (for scripts that need .env)
python-dotenv>=1.0.0
